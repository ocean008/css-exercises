name: Green Tick Workflow

on: [push, pull_request]

jobs:
  green-tick-job:
    runs-on: ubuntu-latest

    steps:
      - name: Complete job with green tick
        run: echo "This job will succeed without running checks."
